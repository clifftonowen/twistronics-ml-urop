# Core dependencies for the data-generation pipeline (Phase 1).
# Tested on Python 3.14.4 (Windows) inside .venv.
numpy>=2.0
scipy>=1.13
matplotlib>=3.8
tqdm>=4.66

# --- Phase 2+ (forward surrogate / inverse design) ---
# torch has a Python-3.14 wheel as of 2.12 (verified on this env: 2.12.1+cpu).
torch>=2.12
# scikit-learn>=1.4   # optional: classical baselines / metrics
