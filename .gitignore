CLAUDE.md
AGENTS.md
discussion.md

# Python environment
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/

# Generated datasets (regenerable from the pipeline; can be large)
datasets/raw/
datasets/processed/
# Convergence-study artifacts (CSV/JSON/PNG); the chosen N_m bins are recorded
# in README + data_generation/convergence.py, so these can be regenerated.
datasets/convergence/

# Trained models + eval artifacts (regenerable from the pipeline)
models/artifacts/

# Notebook checkpoints
.ipynb_checkpoints/
